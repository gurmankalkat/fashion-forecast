/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['oaidalleapiprodscus.blob.core.windows.net'], // Add your domain here
    },
  };
  
  export default nextConfig;
  